name: Supabase migrations (duplicate - noop)

# Ce fichier est un doublon de .github/workflows/supabase-migrations.yml
# Il a été transformé en "noop" pour éviter des exécutions/confusions.
# Conserver uniquement .github/workflows/supabase-migrations.yml pour les migrations.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Duplicate workflow placeholder
        run: |
          echo "This workflow was a duplicate and has been replaced by a noop placeholder."
